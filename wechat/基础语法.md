## wx:for

```
wx: for 控制绑定一个数组。索引值为index,变量名为item
//案例
<!—array 是一个数组 -->
<view wx:for=”{{array}}”>
    {{index}} : {{item.message}}
</view>

Page({
    data: {
        array: [{
            message: ‘foo’,
        },
        {
            message: ‘bar’,
        }]
    }
})
```

